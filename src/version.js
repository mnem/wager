/**
 * Build metadata.
 *
 * This file is a committed DEV STUB so the site works when opened locally with
 * no build step. It must stay a stub — never commit real build metadata here.
 *
 * The deploy workflow regenerates this file into the staging directory with the
 * real version, commit SHA and build time. See CLAUDE.md.
 */
export const BUILD = Object.freeze({
  version: 'dev',
  commit: null,
  commitShort: 'local',
  repo: 'mnem/wager',
  builtAt: null,
});
